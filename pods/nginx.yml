apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: demo
spec:
  containers:
    - name: nginx-cont
      image: nginx:latest
      env:
        - name: name
          value: "Mahesh"
      ports:
        - containerPort: 80